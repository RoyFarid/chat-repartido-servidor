# Chat Multihilo – Servidor (Java)

Servidor WebSocket en **Java** usando **Tyrus** (implementación de referencia de Jakarta WebSocket API).

## 🚀 Ejecución

1. Compila y ejecuta:
   ```bash
   mvn clean compile exec:java -Dexec.mainClass="com.chat.server.ChatServer"
